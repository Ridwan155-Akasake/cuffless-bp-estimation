/bp-monitoring-app/
├── public/
│   ├── index.html         ← Landing page
│   ├── measure.html       ← Submits IR, RED, SPO2, HR
│   ├── collect.html       ← Adds user demographics + BP
│   ├── data.html          ← Shows & deletes records
│   ├── about.html         ← Project & team info
│   ├── style.css          ← Combined styling (all pages)
│   └── main.js            ← Unified frontend JavaScript logic
├── .env                   ← MongoDB credentials (not uploaded)
├── index.js               ← Express backend server
├── package.json           ← Node.js config (dependencies)
└── README.md              ← (Optional: write your project description)





/bp-monitoring-app/
├── public/                 ← Static files (HTML, CSS, client-side JS)
│   ├── images              ← image files
│   ├── index.html          ← Landing page
│   ├── measure.html        ← Submits IR, RED, SPO2, HR
│   ├── collect.html        ← Adds user demographics + BP
│   ├── data.html           ← Shows & deletes records
│   ├── about.html          ← Project & team info
│   ├── style.css           ← Combined styling (all pages)
│   └── main.js             ← Unified frontend JavaScript logic
├── api/                    ← Serverless functions (backend)
│   ├── index.js            ← Home API endpoint
│   ├── ppgData/            ← PPG data related API endpoints
│   │   ├── index.js        ← GET all data, POST new data
│   │   ├── latest.js       ← GET/PUT latest data
│   │   └── [id].js         ← PUT/DELETE specific record
├── lib/                    ← Shared backend utilities
│   └── mongodb.js          ← MongoDB connection handler
├── .env.local              ← Local environment variables (not committed)
├── .gitignore              ← Files and folders to ignore in version control
├── vercel.json             ← Vercel configuration
├── package.json            ← Node.js configuration (dependencies)
├── README.md               ← Project description and documentation
└── node_modules/           ← Automatically generated by npm; not committed