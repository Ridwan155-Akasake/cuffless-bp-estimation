// Copy this file to `secrets.h` and fill in your real Wi-Fi credentials.
// `secrets.h` is gitignored — it will not be committed to the repo.

#ifndef SECRETS_H
#define SECRETS_H

const char* WIFI_SSID     = "YOUR_WIFI_SSID_HERE";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD_HERE";

// Backend ingest endpoint (Vercel deployment by default).
const char* BACKEND_URL   = "https://io-t-ppg-bp-monitor-backend.vercel.app/api/ppgData";

#endif
